# Dependencies — reinstalled inside the image via `npm ci`
node_modules

# Build output — regenerated by `npm run build`
dist
dist-ssr

# Tests & their artifacts — not part of the production build
tests
e2e
coverage
test-results
playwright-report
blob-report
playwright/.cache
.pw-deps

# Version control & CI
.git
.gitignore

# Editor / OS cruft
.idea
.vscode
.DS_Store

# Docker
Dockerfile
.dockerignore

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Agent scratch
.agents
